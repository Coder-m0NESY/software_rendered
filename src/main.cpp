// 占位入口：只为让构建任务能跑通，请直接覆盖这个文件。
// 提示：此错误为 IDE IntelliSense 配置问题，需在项目根目录创建 .vscode/c_cpp_properties.json 或通过 CMakeLists.txt 正确配置 SDL2 的 include 路径
#include <SDL.h>

int main(int argc, char* argv[])
{
    return 0;
}
